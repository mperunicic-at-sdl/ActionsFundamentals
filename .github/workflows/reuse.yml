name: Reuse other workflow

on: [workflow_dispatch]

jobs:
  call-workflow:  
    uses: ./.github/workflows/reusable.yml
    with:
      who-to-greet: '@mperunicic-at-sdl'    
  job-2:
    needs: [call-workflow]
    runs-on: ubuntu-latest    
    steps:
      - run: |
          echo "the value from reusable: ${{ needs.call-workflow.outputs.current-time }}"
        
