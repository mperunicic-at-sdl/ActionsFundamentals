name: Reuse other workflow

on: [workflow_dispatch]

jobs:
  call-workflow:  
    uses: ./.github/workflows/reusable.yml
    with:
      who-to-greet: '@mperunicic-at-sdl'    
  job-2:
    needs: [call-workflow]
    runs-on: ubuntu-latest    
    steps:
      - run: |
          echo "Bla bla ${{ needs.call-workflow.outputs.time }}"
        
